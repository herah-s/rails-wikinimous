<% @articles.each do |article| %>
  <div class='col-8 centered'>
    <%= link_to article.title, article_path(article), class: 'article-title' %>
    <p class='article-text' limit='100'><%= article.content %></p>
  </div>
<% end %>
